// import tailwindcss from "@tailwindcss/postcss"; // No longer needed if using string key

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
    // If you use autoprefixer, you would add it here as well:
    // 'autoprefixer': {},
  },
};

export default config;
